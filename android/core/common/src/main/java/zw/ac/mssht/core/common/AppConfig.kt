package zw.ac.mssht.core.common

object AppConfig {
    /** Emulator loopback to host Apache/XAMPP. Override via BuildConfig in :app. */
    const val DEFAULT_BASE_URL = "http://10.0.2.2/mssht/"
    const val API_PREFIX = "api/mobile/v1/"
}
